pipeline {

    agent any

    stages {

        stage("build") {

            steps {
                echo 'building the containers'
            }
        }

        stage("upload") {

            steps {
                echo 'pushing images to registry'
            }
        }
    }
}

node {

}